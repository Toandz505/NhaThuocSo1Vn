@model IEnumerable<GameStore.Models.DanhMuc>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/layoutAdmin.cshtml";
}

<h2>Quản lý danh mục</h2>

<p>
    @Html.ActionLink("Thêm danh mục", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.tenDM)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.tenDM)
        </td>
        <td>
            @Html.ActionLink("sửa", "Edit", new { id=item.maDM }) |
            @Html.ActionLink("xem", "Details", new { id=item.maDM }) |
            @Html.ActionLink("xóa", "Delete", new { id=item.maDM })
        </td>
    </tr>
}

</table>
